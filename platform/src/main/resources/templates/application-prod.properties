# --- Production Profile Configuration ---

# Hibernate will only validate the schema, preventing accidental database drops/updates
spring.jpa.hibernate.ddl-auto=validate

# Hide SQL queries to prevent database structure exposure in logs
spring.jpa.show-sql=false

# Restrict logging to WARN to guarantee no sensitive user data is logged (LOW-02)
logging.level.com.ctf.platform=WARN