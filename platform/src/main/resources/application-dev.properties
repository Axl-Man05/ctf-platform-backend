# --- Development Profile Configuration ---

# Hibernate will update the database schema automatically
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in the console to help with local debugging
spring.jpa.show-sql=true

# Detailed application logging (LOW-02: Only active in DEV mode)
logging.level.com.ctf.platform=DEBUG